import Photo from "./Photo";
// import { Stack } from "./styled";

export default function PhotoSection({ row, index }) {
  return (
    // <Stack justify='flex-start' spacing='20px'>
    //   {row.map((photo) => {
    //     return <Photo key={photo.id} {...photo} />;
    //   })}
    // </Stack>
    <div>Photo</div>
  );
}
